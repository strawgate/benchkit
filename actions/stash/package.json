{
  "name": "@benchkit/stash",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action: parse benchmark results and commit to a data branch.",
  "scripts": {
    "build": "tsc && ncc build lib/main.js -o dist -s",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.0",
    "@actions/exec": "^1.1.1",
    "@actions/glob": "^0.5.0",
    "@benchkit/format": "*"
  }
}
