{
  "name": "@benchkit/compare",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action: compare PR benchmark results against baseline and post a PR comment.",
  "scripts": {
    "build": "tsc && ncc build lib/main.js -o dist -s",
    "test": "tsc && node --test lib/compare-action.test.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.0",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "@actions/glob": "^0.5.0",
    "@benchkit/format": "*"
  }
}
