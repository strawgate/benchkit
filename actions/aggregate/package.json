{
  "name": "@benchkit/aggregate",
  "version": "0.1.1-beta.0",
  "private": true,
  "description": "GitHub Action: rebuild index and series files from benchmark runs.",
  "scripts": {
    "build": "tsc && ncc build lib/main.js -o dist -s",
    "test": "tsc && node --test lib/aggregate.test.js lib/integration.test.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.0",
    "@actions/exec": "^1.1.1",
    "@benchkit/format": "*"
  }
}
