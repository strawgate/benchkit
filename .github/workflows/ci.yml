name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Lint
        run: npx eslint .

      - name: Validate schemas
        run: node --test schema/schema.test.mjs

      - name: Build all packages and actions
        run: npm run build

      - name: Test format package
        run: npm run test --workspace=packages/format

      - name: Test chart package
        run: npm run test --workspace=packages/chart

      - name: Test aggregate action
        run: npm run test --workspace=actions/aggregate

      - name: Test monitor action
        run: npm run test --workspace=actions/monitor

      - name: Verify action bundles are up-to-date
        run: |
          if [[ -n "$(git status --porcelain actions/*/dist/)" ]]; then
            echo "::error::Action dist/ bundles are out of date. Run 'npm run build' and commit."
            git diff --stat actions/*/dist/
            exit 1
          fi
